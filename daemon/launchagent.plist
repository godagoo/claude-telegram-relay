<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Unique identifier for this service -->
    <key>Label</key>
    <string>com.claude.telegram-relay</string>

    <!-- Command to run -->
    <key>ProgramArguments</key>
    <array>
        <!-- UPDATE THIS: Path to bun -->
        <string>/Users/YOUR_USERNAME/.bun/bin/bun</string>
        <string>run</string>
        <string>src/relay.ts</string>
    </array>

    <!-- Working directory -->
    <key>WorkingDirectory</key>
    <!-- UPDATE THIS: Path to your relay directory -->
    <string>/Users/YOUR_USERNAME/claude-telegram-relay</string>

    <!-- Environment variables -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <!-- UPDATE THIS: Include paths to bun, node, claude -->
        <string>/Users/YOUR_USERNAME/.bun/bin:/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/local/sbin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>HOME</key>
        <!-- UPDATE THIS -->
        <string>/Users/YOUR_USERNAME</string>
        <key>CLAUDE_PATH</key>
        <!-- UPDATE THIS: launchd does not inherit your interactive shell PATH -->
        <string>/Users/YOUR_USERNAME/.local/bin/claude</string>
        <key>CLAUDE_TIMEOUT_MS</key>
        <string>90000</string>
        <key>CLAUDE_RESUME</key>
        <string>0</string>
    </dict>

    <!-- Start when loaded -->
    <key>RunAtLoad</key>
    <true/>

    <!-- Keep running (restart if it crashes) -->
    <key>KeepAlive</key>
    <true/>

    <!-- Log files -->
    <key>StandardOutPath</key>
    <!-- UPDATE THIS -->
    <string>/Users/YOUR_USERNAME/Library/Logs/claude-telegram-relay.log</string>

    <key>StandardErrorPath</key>
    <!-- UPDATE THIS -->
    <string>/Users/YOUR_USERNAME/Library/Logs/claude-telegram-relay.error.log</string>

    <!-- Wait 10 seconds before restarting after crash -->
    <key>ThrottleInterval</key>
    <integer>10</integer>
</dict>
</plist>

<!--
SETUP INSTRUCTIONS:

1. Replace all YOUR_USERNAME with your actual macOS username

2. Prefer the generated installer, which keeps PATH, logs, RELAY_DIR, and
   launchctl bootstrap behavior in sync:
   bun run setup:launchd -- --service relay

   Manual fallback: copy to LaunchAgents:
   cp daemon/launchagent.plist ~/Library/LaunchAgents/com.claude.telegram-relay.plist

3. Make sure your .env file exists in the relay directory

4. Load the agent:
   launchctl load ~/Library/LaunchAgents/com.claude.telegram-relay.plist

5. Check if running:
   launchctl list | grep claude

6. View logs:
   tail -f ~/Library/Logs/claude-telegram-relay.log

7. To stop:
   launchctl unload ~/Library/LaunchAgents/com.claude.telegram-relay.plist

8. To restart:
   launchctl unload ~/Library/LaunchAgents/com.claude.telegram-relay.plist
   launchctl load ~/Library/LaunchAgents/com.claude.telegram-relay.plist
-->
